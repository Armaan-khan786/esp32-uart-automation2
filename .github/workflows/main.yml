name: ESP32 UART Toggle Test

on:
  push:

jobs:
  uart-test:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Python dependency
        run: pip install pyserial

      - name: Run ESP32 UART test
        run: run_uart_test.bat
